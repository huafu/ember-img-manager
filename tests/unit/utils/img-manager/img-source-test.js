import imgManagerImgSource from 'ember-img-manager/utils/img-manager/img-source';

module('imgManagerImgSource');

// Replace this with your real tests.
test('it works', function() {
  var result = imgManagerImgSource();
  ok(result);
});
