import imgManagerImgRule from 'ember-img-manager/utils/img-manager/img-rule';

module('imgManagerImgRule');

// Replace this with your real tests.
test('it works', function() {
  var result = imgManagerImgRule();
  ok(result);
});
